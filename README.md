Start the Development container. 
Once inside run  (-o for open browser and -l for live reload)

`bash
    > ./run.sh
`

Resume renders with cards in brower, but prints to PDF with simple bullet style.
